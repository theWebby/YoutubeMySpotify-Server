version: 0.2

phases:
  build:
    commands:
       - echo $(git tag --points-at HEAD)
       - export TAG=$(git tag --points-at HEAD)
       - cd lambda_spike
       - npm install --quiet
       - |
         if grep -q release <<< $TAG; then
            npm run deploy-prod
         else
            npm run deploy-dev
         fi
